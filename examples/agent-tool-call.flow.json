{
  "schema_version": 1,
  "type": "flow",
  "meta": {
    "title": "Agent 工具调用流程",
    "caption": "图 7-2 · Agent 工具调用流程（实测意图识别 1.2s，平均重试 0.3 次）",
    "locale": "zh-CN",
    "viewBox": [800, 500]
  },
  "nodes": [
    { "id": "ask", "label": "用户提问", "sublabel": "UserQuery", "role": "interaction", "kind": "start" },
    { "id": "intent", "label": "意图识别", "sublabel": "IntentClassifier", "role": "control", "kind": "decision" },
    { "id": "exec", "label": "工具执行", "sublabel": "ToolExecutor", "role": "capability", "kind": "step" },
    { "id": "verify", "label": "结果校验", "sublabel": "ResultValidator", "role": "control", "kind": "decision" },
    { "id": "reply", "label": "返回答案", "sublabel": "Answer", "role": "interaction", "kind": "terminal" }
  ],
  "edges": [
    { "from": "ask", "to": "intent", "label": "输入", "kind": "sync" },
    { "from": "intent", "to": "exec", "label": "可调用工具", "kind": "sync" },
    { "from": "exec", "to": "verify", "label": "结果", "kind": "sync" },
    { "from": "verify", "to": "reply", "label": "校验通过", "kind": "sync" },
    { "from": "verify", "to": "exec", "label": "校验失败·重试", "kind": "fallback" }
  ]
}
